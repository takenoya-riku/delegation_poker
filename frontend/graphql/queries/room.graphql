query Room($code: String!) {
  room(code: $code) {
    id
    name
    code
    participants {
      id
      name
    }
    topics {
      id
      title
      description
      status
      allParticipantsVoted
      votes {
        id
        level
        voteType
        participant {
          id
          name
        }
      }
    }
  }
}
